# Database Configuration
DB_HOST=your-db-host
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_DATABASE=your-db-name

# Session Secret
SESSION_SECRET=your-session-secret

# Port (Netlify will use its own port in production)
PORT=3000
